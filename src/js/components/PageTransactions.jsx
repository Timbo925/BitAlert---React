const React = require('react');

let PageTransactions = React.createClass({
  getInitialState() {
    return {};
  },

  componentDidMount() {
  },

  render() {
    return (
      <p>Hello, world!</p>
    );
  }
});

module.exports = PageTransactions;
